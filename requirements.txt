# API server
fastapi==0.135.1
uvicorn==0.42.0

# Bayesian modeling (fit_models.py — run locally, not on EC2)
pymc-marketing
pymc-extras
arviz

# Data
pandas
numpy

# AWS (loader.py S3 path, bedrock.py)
boto3

# HTTP client for health checks / testing
httpx
